# In local development: leave as /api to leverage Vite's local dev proxy
# In production (e.g. Vercel, Netlify): set to your backend API URL with /api suffix:
# VITE_API_URL=https://your-backend-app.onrender.com/api
VITE_API_URL=/api
